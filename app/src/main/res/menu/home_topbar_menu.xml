<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:tools="http://schemas.android.com/tools"
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">
    <item android:id="@+id/itemSearch"
        android:icon="@drawable/ic_baseline_search_24"
        android:title="Search"
        app:showAsAction="ifRoom"
        tools:ignore="HardcodedText"
        app:actionViewClass="android.widget.SearchView"
        />

    <item android:id="@+id/itemMe"
        android:title="My Information"
        android:icon="?attr/icon"
        android:actionLayout="@layout/activity_home"
        app:showAsAction="ifRoom"
        tools:ignore="HardcodedText">
        <menu>
            <item android:id="@+id/itemEditInfo"
                android:enabled="true"
                android:title="Edit profile"
                tools:ignore="HardcodedText"
                android:icon="@drawable/ic_baseline_account_circle_24"
                />
            <item
                android:id="@+id/itemSelectTags"
                android:enabled="true"
                android:title="Select tags"
                tools:ignore="HardcodedText"
                android:icon="@drawable/ic_baseline_layers_24"
                />
            <item
                android:id="@+id/itemLogout"
                android:enabled="true"
                android:title="Logout"
                tools:ignore="HardcodedText"
                android:icon="@drawable/ic_baseline_login_24"
                />
        </menu>

    </item>

    <item android:id="@+id/itemMore"
        android:title="More"
        app:showAsAction="never"
        tools:ignore="HardcodedText"
        />
</menu>